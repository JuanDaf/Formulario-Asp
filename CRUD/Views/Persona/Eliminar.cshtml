@model Persona

<div class="row">
   
        <h2 class="">
            Eliminar Personas 
        </h2>
           
         <div class="container-fluid align-content-center">
        
              <form asp-action="Eliminar" asp-controller="Persona" method="post">
                 <div class="row ">
                    <div class="col-12 align-self-center">
                        <div class="form-floating mb-3 col-md-5">
                            <input type="hidden"  asp-for="Cedula" class="form-control" id="floatingInput" placeholder="name@example.com">
                            Desea eliminar a la persona: @Html.DisplayTextFor(m => m.Nombres) ? 
                        </div>
                    </div>
                 </div>
                 <div class="d-grid gap-2 col-5">
                 <button type="submit" class="btn btn-danger" >Eliminar</button>
                 <a asp-action="Index" asp-controller="Persona"class="btn btn-warning" >Volver a la lista</a>
                 </div>
              </form>
         
         </div>
 
</div>
